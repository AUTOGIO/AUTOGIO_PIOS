# Copy to .pios_wifi.env (gitignored) and fill in SSIDs/passwords.
# Never commit the real file.

PIOS_WIFI_SSID=PIOS-HOME
PIOS_WIFI_PASSWORD=change-me
PIOS_IOT_SSID=PIOS-IOT
PIOS_IOT_PASSWORD=change-me
